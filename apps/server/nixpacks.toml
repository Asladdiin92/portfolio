[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["npm install -g pnpm@8.15.0"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @portfolio/shared build || true",
  "pnpm --filter @portfolio/server build"
]

[start]
cmd = "node dist/server.js"
