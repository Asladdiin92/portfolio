[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @portfolio/shared build || true",
  "pnpm --filter @portfolio/server build"
]

[start]
cmd = "node dist/server.js"
